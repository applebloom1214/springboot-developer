spring:
  jpa:
  # 전송 쿼리 확인
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  # 테이블 생성 후에 data.sql 실행
    defer-datasource-initialization: true
#  datasource: # 데이터베이스 정보 추가
#    url: jdbc:h2:mem:testdb
#    username: sa
  h2: # H2 콘솔 활성화
    console:
      enabled: true
  security: # oauth
    oauth2:
      client:
        registration:
          google:
            client-id: 999155991854-tqfjdilrls74sq3sb1sid6pd46v39uve.apps.googleusercontent.com
            client-secret: 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
            scope:
              - email
              - profile
  sql:
    init:
      mode: always
      encoding: UTF-8
jwt:
  issuer: ajufresh@gmail.com
  secret_key: study-springboot